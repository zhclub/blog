server:
  port: 8081
  servlet:
    session:
      timeout: 3600
  compression:
    enabled: true
  mime-types: text/javascript,application/javascript,text/css

spring:
  datasource:
    druid:
      url: jdbc:mysql://11.239.172.20:3306/balabala?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: root
      password: caipiao
      driver-class-name: com.mysql.jdbc.Driver
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true

mybatis:
  mapper-locations: classpath:/mybatis/*.xml